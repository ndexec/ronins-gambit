# Client (Vite) — required for auth and Supabase in the browser
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_PUBLISHABLE_KEY=your-anon-public-key

# Optional SSR fallbacks (same values; used if import.meta.env is unset on the server)
# SUPABASE_URL=
# SUPABASE_PUBLISHABLE_KEY=
